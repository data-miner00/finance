# Finance

## Getting Started

For prod starter, first deploy the Database and Azurite first. Then, use SQL Server Management Studio to connect to Database. Then create database `Finance` manually. Then, open Visual Studio and schema compare the `Database` project with the newly created Database. Then update the Database. Then, build and deploy the WebAPI and then the WebApp.
